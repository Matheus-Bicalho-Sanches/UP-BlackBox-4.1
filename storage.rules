rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Permite upload e leitura apenas para usuários autenticados
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
} 